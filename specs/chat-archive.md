# Chat Archive — Save Command for Claude Desktop

## Status: Merged into memory-v2.md

Subsumed by the `save_chat` MCP tool in `specs/memory-v2.md` (Phase 2). The `save_chat` tool accepts a conversation transcript, runs the shared extraction prompt, and stores patterns through the same dedup pipeline used by `remember`. See memory-v2.md for full spec.
